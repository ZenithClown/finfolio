/**
 * Application Global Constant(s) Configuration
 *
 * Change the constants value, or fetch the value from environment
 * variables when the code is pushed to production.
 */

export const API_BASE_URI = "http://localhost:3100";
export const API_RPC_BASE = `${API_BASE_URI}/rpc`;
