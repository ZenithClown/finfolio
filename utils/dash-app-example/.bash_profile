# a set of variables defined system-wide for the application to run,
# * or the application runs with the default settings wherever possible
export DASH_APPLICATION_PORT = 8050
export DASH_APPLICATION_HOST = localhost
